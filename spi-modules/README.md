# SPI Modules

This directory contains the Keycloak Server Provider Interface (SPI) modules delivered by this repository.
Use the root `README.md` for module-specific configuration details and build steps.
